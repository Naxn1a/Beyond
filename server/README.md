# Beyond - Server

# dotenv
```
# database
DB_USER=
DB_PASS=
DB_HOST=
DB_PORT=
DB_NAME=
DATABASE_URL=postgres://${DB_USER}:${DB_PASS}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# default admin
ADMIN_USERNAME=
ADMIN_EMAIL=
ADMIN_PASSWORD=

# Secret keys
JWT_SECRETS=
```